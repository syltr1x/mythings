alias ls="lsd"
alias cat="bat"
alias torctl="torbrowser-launcher"
#Git Alias
alias gss="git status -s"
alias glo="git log --oneline"
alias gaa="git add ."
alias gpl="git pull"
alias gf="git fetch"
alias mkh="mkdir logs victim" 

gsc() {git clone https://github.com/syltr1x/$1}
gcr() {git clone https://github.com/$1/$2}
gmc() {git commit -m $1}
gps() {git push origin $1}

scan() {nmap -sC -sV -p- --min-rate 1000 $1 -o logs/scan.txt}
